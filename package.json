{
	"name": "haus-search-extension",
	"version": "1.0.0",
	"description": "Chrome extension for Haus Search",
	"scripts": {
		"build": "node scripts/build.js",
		"zip": "npm run build && node scripts/zip.js"
	},
	"devDependencies": {
		"archiver": "^6.0.0"
	}
}
